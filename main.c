#include<stdio.h>

typedef int DataType;



constructor(DataType* the_array);
destructor(DataType* the_array);
push_back(DataType* the_array, DataType element);
pop_back(DataType* the_array);
insert(DataType* the_array, unsigned index, DataType element);
delete(DataType* the_array, unsigned index);
clear(DataType* the_array);
assign(DataType* the_array, DataType the_array2);
get_element(DataType* the_array, unsigned index, DataType* element_to_return);


int main() 
{
}